<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bj4.yhh.changewp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:name=".ChangeWpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".imagesource.ImageSourceActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.main.MainPreferenceActivity"
            android:label="@string/action_settings"
            android:theme="@style/Theme.AppCompat" />

        <activity
            android:name=".externalstorage.ExternalStorageAlbumActivity"
            android:label="@string/source_type_external_storage_text"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".wallpaper.scheduler.WallpaperJobService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".wallpaper.BootCompletedReceiver"
            android:enabled="false">
            <action android:name="android.intent.action.BOOT_COMPLETED" />
        </receiver>

        <service android:name=".wallpaper.ChangeWallpaperService" />
    </application>

</manifest>